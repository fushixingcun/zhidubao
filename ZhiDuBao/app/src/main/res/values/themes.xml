<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--parent="@android:style/Theme.Holo.Light.DarkActionBar"-->
    <!-- 应用于程序或者活动的主题 -->
    <style name="CustomActionBarTheme"
        parent="@android:style/Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <!-- 设置overflow按钮的图片资源 -->
        <item name="android:actionOverflowButtonStyle">@style/MyOverFlowButton</item>
        <!-- 设置overflow中items的背景和点击选中时的style -->
        <item name="android:dropDownListViewStyle">@style/MyDropDownListView</item>
        <item name="android:homeAsUpIndicator">@mipmap/ic_action_back</item>
    </style>
    <!-- ActionBar 样式 -->
    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">#18B4ED</item>
        <!--设置ActionBar的标题的样式-->
        <item name="android:titleTextStyle">@style/ActionBarTextStyle</item>
    </style>
    <!--Android3.0及以上ActionBar字体样式 -->
    <style name="ActionBarTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">#FFFFFF</item>
    </style>
    <!--Android3.0及以上设置overflow按钮的图片资源 -->
    <style name="MyOverFlowButton" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@mipmap/ic_action_overflow</item>
    </style>
    <!--Android3.0及以上设置overflow中items的背景和点击选中时的style-->
    <style name="MyDropDownListView" parent="@android:style/Widget.Holo.Light.ListView.DropDown">
        <item name="android:listSelector">#18B4ED</item>
        <item name="android:background">#FFFFFF</item>
    </style>
</resources>